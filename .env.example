# ==========================================================
# School Management System - root environment example
# Copy to .env before running docker-compose
# ==========================================================

# MySQL
MYSQL_DATABASE=school_db
MYSQL_USER=school_user
MYSQL_PASSWORD=school_pass
MYSQL_ROOT_PASSWORD=root_secret

# Backend
NODE_ENV=development
PORT=3000
JWT_SECRET=change_this_jwt_secret_in_production
JWT_REFRESH_SECRET=change_this_refresh_secret_in_production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

DB_HOST=mysql
DB_PORT=3306

# CORS origin for the Angular frontend
CORS_ORIGIN=http://localhost

# Redis (optional - caching / rate limiting store)
REDIS_HOST=redis
REDIS_PORT=6379

# SMTP (email notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=no-reply@school.local

# SMS / Twilio (optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Frontend URL (for emails / links)
FRONTEND_URL=http://localhost